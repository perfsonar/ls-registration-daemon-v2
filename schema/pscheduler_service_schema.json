{
    "$id": "https://github.com/perfsonar/ls-registration-daemon-v2/blob/schema/schema/pscheduler_service_schema.json",
    "title": "perfSONAR Lookup Service's Pscheduler Service Schema",
    "type": "object",
    "allOf": [
        {"$ref": "service_schema.json"}
    ],
    "properties": {
        "tools": {
            "description": "pscheduler tools installed",
            "type": "array",
            "items": {
              "type": "string"
            }
        },
        "tests": {
            "description": "pscheduler tests enabled on the host",
            "type": "array",
            "items": {
              "type": "string"
            }
        }
    },
    "required": ["tools", "tests"],
    "additionalProperties": false
}